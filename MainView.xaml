<Window x:Class="multiangular.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:multiangular"
             mc:Ignorable="d" 
             Height="200" Width="200">
    <StackPanel>
        <Label Content="Четное количнство граней" Margin="5"/>
        <TextBox Text="{Binding Quantity}" Margin="5"/>


        <Label Content="Диаметер"/>
        <TextBox Text="{Binding Diameter}" Margin="5"/>
        <Button Content="Ok!"
                Command="{Binding SaveCommand}"
                Margin="20"/>
    </StackPanel>
</Window>
